<android.support.v4.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_event_swipe_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/daily_events_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="none"/>

        <android.support.v7.widget.CardView
            android:id="@+id/calendar_frame"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            card_view:cardElevation="@dimen/caldroid_elevation"
            card_view:cardMaxElevation="@dimen/caldroid_elevation"
            card_view:cardBackgroundColor="@android:color/transparent">
        </android.support.v7.widget.CardView>

    </FrameLayout>

</android.support.v4.widget.SwipeRefreshLayout>